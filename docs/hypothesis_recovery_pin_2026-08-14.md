# CCOC hypothesis-recovery pin — 2026-08-14

## Recovery source snapshot

The repository-bounded hypothesis recovery is pinned to:

`277609bb2da705ba3b7b71029e3f0e44be24600a`

This is the merge commit of PR #202, which contains the actual reverse-recovery pass over issues, PR history, closed-unmerged work, branch-only lineages, scientific docs, early methods/evidence layers, proof-scope corrections, and module ownership.

## Validation provenance

The final PR #202 head before merge was:

`c5e22ef0fb0566954aae8ca1215ad2502d1d64d8`

On that exact head:

- theorem-registry integrity: success;
- full pytest Python 3.10: success;
- full pytest Python 3.11: success;
- full pytest Python 3.12: success.

The PR was documentation/recovery-only; no theorem code or theorem truth status changed.

## Canonical recovery record

Use `docs/hypothesis_recovery_canonical_index_2026-08-14.md` as the entrance. It makes the PR #201 base ledger plus `REC-01`–`REC-51` recovery appendices one canonical record.

The recovery record deliberately includes:

- proved results;
- refuted and corrected hypotheses;
- local/sufficient results whose converse is absent;
- deferred and abandoned research questions;
- unmerged experimental work;
- historical H1–H4 literature hypotheses;
- early evidence/design theorems now outside the first-paper proof spine.

## Active versus recovered-but-deferred

Recovery does not reactivate every historical question.

The current active scientific programs remain:

1. scalable feedback-memory / feedback-portability beyond the five-state benchmark;
2. one real application with sufficient transition/recruitment/movement information to instantiate and falsify a CCOC structural mechanism.

Other unresolved/deferred items—including generic contraction blankets, general stochastic closure calculus, arbitrary multi-valued/concurrent relay compilation, autonomous local delay-gate compilation, richer constrained codebook families, and four-symbol alphabet minimality—remain recovered provenance but non-priority unless explicitly reopened.

## Novelty gate

No novelty conclusion is part of this pin.

The next phase must begin from this pinned hypothesis record and compare publication-relevant rows against prior work one by one. Existing novelty documents produced before the recovery pass remain provisional until reconciled against this pin.

Do not change the recovery source SHA retroactively. If a genuinely omitted repository hypothesis is later discovered, append a dated recovery amendment and create a new pin rather than rewriting this historical record.