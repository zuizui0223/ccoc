# Legacy shelf: retained non-publication theorem branches

These finite results are retained as executable historical material, but they are
**not part of the active open-composition manuscript**. They remain available for
replay, comparison, and later promotion; this shelf is a freeze, not a statement
that any result is false.

The current manuscript is restricted to:

1. exact grammar-aware interfaces;
2. extension--compression noncommutation under operational addressability;
3. the bounded-degree relay sharpness realization; and
4. conservative macro schemas with their local fiber-split boundary.

See the root [legacy manifest](../../legacy/manifest.md) for the complete
compatibility-preserving migration map.

| Archived branch | Registry group | Why retained | Why outside the manuscript |
|---|---|---|---|
| Finite closure classification | `CORE-0` | prerequisite for some declared finite maps | does not enter the open-composition lower bound or sharpness construction |
| Non-nested replacement and rewiring | `EXT-1`--`EXT-4` | coherent transport and target-only action results | would add a second theorem about stage replacement to the paper |
| Delayed exposure and finite-evidence limits | `ID-1` | clear boundary on what monitoring can establish | concerns evidence/identifiability, not declared-grammar portability |
| Candidate and joint exterior-mechanism laws | `ID-2`, `ID-3` | records uncertainty-aware companion mathematics | not a premise of the addressability lower bound |
| Budgeted delayed joint quotients | `LEGACY-1` | exact partial-identification ladder for reset-panel contracts | depends on predeclared reset and budget protocols |
| Witnessed boundary evidence | `LEGACY-1` | lower-bound bookkeeping without coverage | evidence bookkeeping, not compression theory |
| Robust and common-mode canonical panels | `LEGACY-1` | cell-loss and failure-architecture results | measurement design after a quotient is fixed |
| Historical examples and benchmarks | no active registry ID | preserves development and reproducibility context | not part of a theorem proof or the manuscript supplement |

## Rules

1. Do not add features, headline claims, workflows, or examples to this shelf
   during manuscript preparation.
2. Do not re-export legacy material from `causal_model.portability_core`.
3. `causal_model.identifiability_companion` and `causal_model.current_theory`
   remain compatibility surfaces only; neither is an active research entrance.
4. A future promotion requires a precise statement of which active assumption or
   conclusion changes. A useful application story alone is insufficient.
5. Keep deterministic legacy replays available, but do not let their maintenance
   expand the paper's claim.

Historical source paths remain temporarily stable. A physical move under
`causal_model.legacy` is scheduled only after the manuscript source release is
tagged, so pre-submission provenance and old imports are not changed at once.
