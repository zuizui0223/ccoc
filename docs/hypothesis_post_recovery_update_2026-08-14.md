# Post-recovery hypothesis status update — 2026-08-15

> This document updates theorem status after the pinned hypothesis-recovery source snapshot. It does **not** modify the historical recovery pin and it is not a novelty adjudication.

## HYP-I04 — endogenous feedback-network portability

At the recovery pin this was an open candidate based only on a five-state benchmark. The finite deterministic persistent-mode program is now substantially closed through five merged theorem layers:

1. **PR #204 — scalable feedback-gate rank.** An explicit mode → turnover-induced facilitator loss → future accessibility cycle can generate exactly `r` hidden feedback bits; removing either causal arrow collapses the burden to zero.
2. **PR #205 — bounded-type feedback portability.** Copy-anonymous physical replication is compressible: one interaction type has a canonical five-state quotient independent of replication and fixed `q` types give one `5^q`-state changing-domain macro.
3. **PR #207 — evolving master feedback types.** Context-dependent type maps close through a master signature. Bounded instantaneous type count is insufficient: a rotating family has two types per context but `2^r` master classes.
4. **PR #208 — future-context causal forgetting.** Under autonomous context evolution, retain only distinctions in contexts still reachable from the present. Future feedback rank is non-increasing and an irreversible chain realizes exact memory `r,r-1,...,0`.
5. **PR #210 — state/mode-dependent context continuation closure.** Hidden mode may now rewrite successor ecological context itself. Monotone continuation refinement reaches a finite fixed point and yields the unique minimum hidden-mode repair among exact interfaces that retain ecological context and macrostate explicitly. The routed-context equality family has at most two instantaneous types and only `3r+1` contexts but exactly `2^r` initial continuation classes and sharp exposure/stabilization depth `2r-1`.

### Current deterministic status

Within the declared finite deterministic class with persistent hidden mode and explicit ecological `(context, macrostate)` coordinates, the minimum additional hidden-mode memory is therefore **RESOLVED BY A GENERAL RELATIVE CLOSURE THEOREM**.

The simpler PR #204/#205/#207/#208 results should now be treated as interpretable closed forms, corollaries, and sharpness/obstruction families around PR #210 rather than separate unresolved programs.

Use `docs/feedback_portability_theorem_family_2026-08-15.md` as the consolidated entrance.

### What would count as a genuinely new HYP-I04 extension

Do not reopen HYP-I04 merely with another deterministic graph/type/context witness. A new theorem must change a model premise materially, for example:

- hidden interaction mode itself evolves;
- stochastic feedback kernels;
- partial observation / uncertain context;
- continuous or unbounded state with a nontrivial finite structural bound;
- a new quantitative approximation/resource law not reducible to PR #210 or existing information substrate.

These are **not currently active by default**.

## HYP-J01 — real mechanism application

Still open and now the main scientific continuation on the feedback/application side. No currently identified dataset supplies the transition/recruitment/disturbance/movement layer needed to identify the exact CCOC mechanisms rather than only associations.

## HIST-H1–H4

Unchanged by the feedback mathematics. They remain historical prior-literature gates for first-paper bounded-local realization wording.

## Novelty status

No novelty conclusion is added here. The deterministic feedback family is mathematically fixed enough for later row/package-level comparison, but historical novelty must still be audited separately.
