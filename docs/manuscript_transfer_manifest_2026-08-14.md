# Open-composition manuscript transfer manifest — reduced surface

## Editorial decision

The first CCOC paper transfers only the theorem spine needed to answer one question:

> Why can exact compression valid in each declared closed future fail to provide one comparably small exact interface after the legal future grammar is opened?

## Main handoff

### Foundation

- `dynamic_boundary_blankets.py`
- `shared_grammar.py`
- `grammar_aware_blankets.py`
- `addressable_completion_bounds.py` — retained because CORE-1's uniform-blanket obstruction directly depends on its canonical addressable product; it is not an independent headline claim

### Headline lower bound

- `extension_compression.py`
- `extension_compression_noncommutation.py`
- `operational_addressability.py`

### Sharpness / constrained realization

- `relay_tree_compilation.py`
- `constant_alphabet_relay.py`
- `fixed_regular_grammar_relay.py`
- `extremal_open_composition.py`
- `local_causal_cone.py`

### Positive/negative boundary

- `coherent_portable_macrolaw.py`
- `conservative_macro_schema.py`

## Supporting strengthening

The manuscript may cite, preferably after the core theorem rather than as equal-weight results:

- `addressable_codebooks.py`
- `codebook_families.py`
- `docs/addressable_codebook_bound.md`
- `docs/composition_code_rate.md`

These show that large open-interface inflation does not require a full independent Cartesian product of exterior states.

## Stronger-model follow-up

`approximate_addressability.py` and `docs/approximate_addressability.md` are retained outside the core spine because they replace exact recovery with bounded-error recovery. Do not present Fano's inequality or approximate abstraction as new.

## Historicalized material

Exact converse/reuse, generic canonical quotient branches beyond the retained foundation dependency, resource-accounting, observation-window, stochastic/ecological special-case, and other removed branches are Git-history provenance, not manuscript dependencies.

The pre-removal recovery pin for this final internal-side-branch pass is:

`0d3424a9090b86eae4e369d3749bbe39b6b03432`.

## Claim boundary

Do not claim novelty for fixed-grammar minimization, partition refinement, generic locality, generic code counting, Fano/information inequalities, or universal sequential-machine compilation.

The residual first-paper claim remains the cross-grammar exact-interface separation plus its constrained sharpness realization. Historical priority for the bounded-local compilation package remains subject to the H1–H4 primary-source audit.

## Transfer rule

At manuscript transfer, pin the exact final CCOC commit SHA and replay:

```bash
python scripts/verify_theorem_registry.py --check --write-report
python scripts/verify_paper_core.py --write-report
pytest -q
```

Do not cite `main` or `latest` as proof provenance.
