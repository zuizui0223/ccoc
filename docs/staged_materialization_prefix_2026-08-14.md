# Staged materialization prefix theorem

For nested requirements S_t, every stage prefix must satisfy a cumulative information bound. In the exact binary power-of-two subclass, these prefix bounds are also sufficient by earliest-deadline assignment.
