name: Delayed joint reset-panel certificate replay

on:
  pull_request:
    paths:
      - 'causal_model/delayed_joint_nonidentifiability.py'
      - 'causal_model/delayed_joint_reset_panels.py'
      - 'scripts/verify_delayed_joint_reset_panels.py'
      - 'tests/test_delayed_joint_reset_panels.py'
      - 'docs/delayed_joint_reset_panels.md'
      - '.github/workflows/delayed-joint-reset-panels.yml'
  push:
    branches: [main]
    paths:
      - 'causal_model/delayed_joint_nonidentifiability.py'
      - 'causal_model/delayed_joint_reset_panels.py'
      - 'scripts/verify_delayed_joint_reset_panels.py'
      - 'tests/test_delayed_joint_reset_panels.py'
      - 'docs/delayed_joint_reset_panels.md'
      - '.github/workflows/delayed-joint-reset-panels.yml'

permissions:
  contents: read

jobs:
  certificate-replay:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - name: Install test dependencies
        run: python -m pip install --upgrade pip pytest
      - name: Run reset-panel complexity tests
        run: python -m pytest tests/test_delayed_joint_reset_panels.py -q
      - name: Write deterministic replay report
        run: >-
          PYTHONPATH=. python scripts/verify_delayed_joint_reset_panels.py
          --max-exterior-port-count 4
          --max-delay 4
          --output artifacts/delayed_joint_reset_panels_report.json
      - name: Upload theorem report
        uses: actions/upload-artifact@v4
        with:
          name: delayed-joint-reset-panels-report
          path: artifacts/delayed_joint_reset_panels_report.json
          if-no-files-found: error
