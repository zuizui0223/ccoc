[build-system]
requires = ["setuptools>=64", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "rach-causal-invariants"
version = "0.1.0"
description = "Finite theorem certificates for causal compression under open composition."
readme = "README.md"
requires-python = ">=3.10"
dependencies = []

[project.optional-dependencies]
dev = ["pytest>=8.0"]

[tool.setuptools]
packages = ["causal_model"]

[tool.pytest.ini_options]
testpaths = ["tests"]
addopts = "-q -m 'not legacy'"
markers = [
  "legacy: archived non-publication theorem branch; run explicitly with -m legacy",
]
